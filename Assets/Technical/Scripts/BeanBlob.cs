using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeanBlob : MonoBehaviour
{
    //Handles how Bean Blobs deform and smear
    [SerializeField] float volume = 1.44f;
    public float height = 1.4f;
    [SerializeField] static float maxSmearLength;
    [SerializeField] static float maxSmearTime;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
